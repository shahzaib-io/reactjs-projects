# ReactJS Projects Collection

A collection of React projects for learning,exploring and portfolio building.

## 🚀 Run Locally
Each folder is an independent React app.  
To run one:

```bash
cd into folder
npm install
npm run dev
